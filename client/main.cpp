#include <iostream>

int main()
{
    // Renderer renderer;
    // Inputer inputs;
    // GameEngine engine;
    // Registry registry;
    // SceneManager sceneManager;
    // get scenes

    bool playing = true;
    while (playing)
    {
        // inputs.getInputs();
        // change scene ?
        // registry.applySystems();
        // renderer.render();
    }
    return 0;
}